import Link from "next/link";

export default async function LoginInterception() {
  return (
    <>
      Its not log in page inside /login instead Im inside the
      loginInterception folder
    </>
  );
}
